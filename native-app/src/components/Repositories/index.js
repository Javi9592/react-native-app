import RepositoryList from './RepositoryList';
import { View } from 'react-native';

const Repositories = () => {
  return (
    <View>
      <RepositoryList />
    </View>
  );
};

export default Repositories;
